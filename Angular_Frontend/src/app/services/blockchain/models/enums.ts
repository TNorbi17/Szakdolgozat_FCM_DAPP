export enum UserType {
  Player = 0,
  Team = 1,
}

export enum OfferStatus {
  Pending = 0,
  Accepted = 1,
  Rejected = 2,
}

export enum PlayerPosition {
  Goalkeeper = 0,
  Defender = 1,
  Midfielder = 2,
  Attacker = 3,
}